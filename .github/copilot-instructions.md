# Workspace Rules

- Do not modify responsive layout, breakpoints, column sizing, sidebar behavior, or related CSS unless the user explicitly requests a responsive/layout change.
- Preserve the current responsive behavior when working on unrelated features.
